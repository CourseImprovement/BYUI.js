/**
 * byui(csv).graph({
 * 		graph: {
 * 			margin: {
	 * 			top: #,
	 * 			left: #,
	 * 			bottom: #,
	 * 			right: #
	 * 		},
	 * 		width: #,
	 * 		height: #
 * 		},
 * 		x: {
 * 			format: ''
 * 		},
 * 		y: {
 * 			format: ''
 * 		}
 * })
 */
byui.fn('graph', function(p){
	var params = p;
	if (!window.d3) throw 'Please import d3';

});